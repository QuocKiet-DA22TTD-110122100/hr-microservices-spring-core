spring:
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      password: test-password
  cloud:
    discovery:
      enabled: false

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false