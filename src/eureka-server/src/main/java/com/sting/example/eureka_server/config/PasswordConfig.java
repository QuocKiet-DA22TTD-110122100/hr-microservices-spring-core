package com.sting.example.eureka_server.config;

// PasswordConfig removed — password encoding is handled via {noop} prefix
// in SecurityConfig.userDetailsService(), so no custom PasswordEncoder bean is needed.
